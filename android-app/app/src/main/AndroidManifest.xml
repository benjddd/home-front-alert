<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.attius.homefrontalert">

    <!-- 1. NETWORK: Required for FCM and Home Front Command API Polling -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- 2. LIFELINE: Required to ensure sirens sound even when the device is idle -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 3. LOCATION: Foreground-only access for real-time distance calculations -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 4. UTILITIES: Audio control for the critical alert volume and boot recovery -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 5. HARDWARE: Flexible requirements to maximize device support -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="@string/launcher_name"
        android:icon="@drawable/app_icon"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PikudAlert">
        
        <!-- Main Entry Point -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:exported="false" />

        <activity
            android:name=".TOSActivity"
            android:label="Terms of Conditions"
            android:exported="false" />

        <activity
            android:name=".ThanksActivity"
            android:label="Acknowledgments"
            android:exported="false" />

        <!-- 
             FCM SERVICE: 
             Crucial for receiving rocket alerts via Google's messaging network.
        -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- 
             LIFE-SAFETY SERVICE: 
             Runs in the foreground to provide a backup polling loop and 
             to trigger sirens immediately based on the user's distance.
        -->
        <service
            android:name=".LocalPollingService"
            android:foregroundServiceType="location|dataSync"
            android:exported="false" />

        <!-- Widget Support -->
        <receiver android:name=".StatusWidgetProvider" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- AUTO-REBOOT RECOVERY: 
             Ensures the polling service restarts if the phone reboots during an attack.
        -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
