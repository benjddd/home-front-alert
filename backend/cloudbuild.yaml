steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_AR_HOSTNAME/$PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME:latest', '.']

  # 2. Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '$_AR_HOSTNAME/$PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME:latest']

  # 3. Deploy to Cloud Run (min-instances=1 keeps the polling loop alive 24/7)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '$_SERVICE_NAME'
      - '--image=$_AR_HOSTNAME/$PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME:latest'
      - '--region=$_REGION'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--min-instances=1'
      - '--max-instances=10'
      - '--no-cpu-throttling'
      - '--update-env-vars=NODE_ENV=production,MAP_SERVICE_URL=https://homefront-map-cjnpwpm63q-zf.a.run.app,RELAY_SERVICE_ACCOUNT=344391280523-compute@developer.gserviceaccount.com'

images:
  - '$_AR_HOSTNAME/$PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME:latest'

substitutions:
  _SERVICE_NAME: homefront-backend
  _REGION: me-west1
  _AR_HOSTNAME: me-west1-docker.pkg.dev
  _AR_REPOSITORY: backend-repo

options:
  logging: CLOUD_LOGGING_ONLY
